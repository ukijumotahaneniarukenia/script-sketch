- IN

```
$echo {a..c} | tr ' ' ','
a,b,c
```

- CMD

```
$echo {a..c} | tr ' ' ',' | perl -nlE 'my @a=split(/,/,$_);while(my($idx,$ele)=each @a){print $idx.":".$ele}'
0:a
1:b
2:c
```

- OUT

```
$echo {a..c} | tr ' ' ',' | perl -nlE 'my @a=split(/,/,$_);while(my($idx,$ele)=each @a){print $idx.":".$ele}'
0:a
1:b
2:c
```
