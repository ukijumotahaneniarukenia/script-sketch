CMD

```
$ echo 'abcdef' | jq -Rr 'split("^..\\K";"g") as [$f,$r]|$r,$f'
```

OUT

```
cdef
ab
```

CMD

```
$ echo 'abcdefghijk' | jq -Rr 'def UNKO(first;remain):first,(remain|select(.!=null)|split("^...\\K";"g")) as [$f,$r]|UNKO($f;$r);split("^...\\K";"g") as [$f,$r]|UNKO($f;$r)|select(.!="")'
```

OUT

```
abc
def
ghi
jk
```

CMD

```
$ echo abcdefghi | jq -Rr --arg reg_exp "^...\\K" 'split($reg_exp;"g")'
[
  "abc",
  "defghi"
]
```

```
$ echo 'abcdefghijk' | jq -Rr --arg reg_exp "^...\\K" 'def UNKO(first;remain):first,(remain|select(.!=null)|split($reg_exp;"g")) as [$f,$r]|UNKO($f;$r);split($reg_exp;"g") as [$f,$r]|UNKO($f;$r)|select(.!="")'
abc
def
ghi
jk
```

これでいい

```
$ echo 'abcdefghijk' | grep -Po '.{1,3}'
abc
def
ghi
jk
```


これが汎用化バージョンのはず


```
$ echo 'CafFeLaTTe' | jq -Rr --arg reg_exp "(?=[A-Z])" 'def UNKO(first;remain):if (remain|length)!=0 then (first,(remain|split($reg_exp;"g")|map(select(.!="")) as $item|UNKO($item[0];$item[1:]|join("")))) else first end;split($reg_exp;"g")|.[0] as $f|.[1:]|map(select(.!=""))|join("") as $r|UNKO($f;$r)'

Caf
Fe
La
T
Te
```


```
$ echo '((()())(()))' | jq -Rr --arg reg_exp "(?=\()" 'def UNKO(first;remain):if (remain|length)!=0 then (first,(remain|split($reg_exp;"g")|map(select(.!="")) as $item|UNKO($item[0];$item[1:]|join("")))) else first end;split($reg_exp;"g")|.[0] as $f|.[1:]|map(select(.!=""))|join("") as $r|UNKO($f;$r)|select(.!="")'
(
(
()
())
(
()))
```
