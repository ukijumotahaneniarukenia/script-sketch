- CMD


```
$ echo 👨‍👩‍👧‍👦 | jq -R 'split("") as $a |$a|map(select(test("‍"))) as $b|($a|length)-($b|length)'
4

$ echo 👨‍👩‍👧‍👦 | jq -R 'split("") as $a|$a|map(select(test("[\u200D]"))) as $b|($a|length)-($b|length)'
4

$ echo 👨‍👩‍👧‍👦 | jq -R 'split("") as $a|$a|map(select(test("[\u1680-\u1681\u2000-\u200B\u202F-\u2030\u205F-\u2060\u3000-\u3001]"))) as $b|($a|length)-($b|length)'
7


$ echo 👨‍👩‍👧‍👦 | jq -R 'split("") as $a|$a|map(select(test("[\u1680-\u1681\u2000-\u200D\u202F-\u2030\u205F-\u2060\u3000-\u3001]"))) as $b|($a|length)-($b|length)'
4


$ echo 👨‍👩‍👧‍👦 | jq -R 'split("") as $a|$a|map(select(test("[\u200B-\u200D\uFEFF]"))) as $b|($a|length)-($b|length)'
4
```



teipでもいけた


```
$ echo 👨‍👩‍👧‍👦 | teip -Gog '[\u200d]' -- sed 's/./@/' | ruby -F@ -anle 'p $F.size'
```
