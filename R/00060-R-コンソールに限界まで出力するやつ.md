- CMD

```
time Rscript -e 'options(max.print=999999);s<-c(1:100000);df<-data.frame(t(sapply(s,function(e){return (c(e,paste(rev(as.integer(intToBits(e))),collapse=""),sprintf("%o",e),sprintf("%x",e),sprintf("%X",e)))})));colnames(df)=c("dec","bin","oct","hex","HEX");df'
```
