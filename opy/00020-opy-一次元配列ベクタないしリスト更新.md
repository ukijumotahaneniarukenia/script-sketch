```
先頭要素の単一更新
echo | opy -m numpy 'B:{import numpy as np;};{a=np.arange(4);a[0]=100};E:{print(a)}'
[100   1   2   3]


指定要素の単一更新
echo | opy -m numpy 'B:{import numpy as np;};{a=np.arange(4);a[1]=100};E:{print(a)}'
[  0 100   2   3]


指定要素の複数更新
echo | opy -m numpy 'B:{import numpy as np;};{a=np.arange(4);a[1:3]=np.arange(100,102)};E:{print(a)}'
[  0 100 101   3]


末尾要素の単一更新
echo | opy -m numpy 'B:{import numpy as np;};{a=np.arange(4);a[a.shape[0]-1]=100};E:{print(a)}'
[  0   1   2 100]
```
