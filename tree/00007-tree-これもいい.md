さまり追加

```
$ time tree -a -ugpf -J /etc | sed -r 's;\\;\\\\;g;' | jq '.[]' | jq -f a.jq | jq -r 'select(. != null)|@tsv' | sed '1itype\tname\tmode\tprot\tuser\tgroup\tdirectories\tfiles'
```

さいず追加

```
$ time tree -a -ugpfh -J /etc | sed -r 's;\\;\\\\;g;' | jq '.[]' | jq -f a.jq | jq -r 'select(. != null)|@tsv' | sed '1itype\tname\tmode\tprot\tuser\tgroup\tsize\tdirectories\tfiles'

```

最終修正日時の追加

```
$ tree -a -ugpfhD --timefmt '%Y-%m-%dT%H:%M:%S' -J /etc | sed -r 's;\\;\\\\;g;' | jq '.[]' | jq -f a.jq | jq -r 'select(. != null)|@tsv' | sed '1itype\tname\tmode\tprot\tuser\tgroup\tsize\tlast-modified\tdirectories\tfiles'

```
