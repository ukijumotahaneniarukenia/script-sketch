fn main() {
    // https://doc.rust-lang.org/1.30.0/book/first-edition/primitive-types.html#numeric-types
    let x: u32 = 42; // `x` has type `i32`.
    println!("{:?}", x);
    let y: f32 = 1.0; // `y` has type `f64`.
    println!("{:?}", y);
}
