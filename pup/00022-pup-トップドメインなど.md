- CMD

  - もうちょういうまくやりたい
- オリジナルのトップレベルドメイン

  - tableタグの１つ目

```
cat a.html | pup '.wikitable,.sortable,.jquery-tablesorter,table'| pup ':nth-child(1)'

cat a.html | pup '.wikitable,.sortable,.jquery-tablesorter,table'| pup ':nth-child(1)'|tr -d \\\n|grep -Po '<tr.*?</tr>'|pup 'a'|tr -d \\\n | grep -Po '(?<=<a).*?(?=</a>)'|grep -Po '(?<==").*?(?=")'|grep -A1 -P '/wiki'|grep -vP '\--'|tr ' ' ',' | xargs -n2 | grep -A1 -P '/wiki/\.[a-z]+'|grep -vP '\--|mw-redirect'>a.out


cat a.out

マスタ
cat a.out | awk '{a[$1]=$2}END{for(e in a)print e,a[e]}'|sort
```
